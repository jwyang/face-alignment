
randf = LBFRegModel.ranf;

num_stage = size(randf, 1);
num_lmarks = size(randf, 2);

for s = 1:num_stage
    filepath = strcat('randomforest_', num2str(s), '.bin');
    fip = fopen(filepah, 'wb');
    for l = 1:num_lmarks
        
    end
end